name: Deploy to Netlify

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify build
        run: curl -sS -X POST "${{ secrets.NETLIFY_BUILD_HOOK }}"
