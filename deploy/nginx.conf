server {
    listen 80;
    server_name theimpactfestival.com www.theimpactfestival.com;
    return 301 https://theimpactfestival.com$request_uri;
}

server {
    listen 443 ssl http2;
    server_name www.theimpactfestival.com;

    ssl_certificate /etc/letsencrypt/live/theimpactfestival.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/theimpactfestival.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    return 301 https://theimpactfestival.com$request_uri;
}

server {
    listen 443 ssl http2;
    server_name theimpactfestival.com;

    ssl_certificate /etc/letsencrypt/live/theimpactfestival.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/theimpactfestival.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /var/www/impact-festival;
    index index.html;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location /api/ {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 1M;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|webp|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
        try_files $uri =404;
    }
}
